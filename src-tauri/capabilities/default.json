{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Android comic reader core (desktop shell used only for local development).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "mobile-import:default",
    "app-updater:default"
  ]
}
