{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the local-first reader core.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
