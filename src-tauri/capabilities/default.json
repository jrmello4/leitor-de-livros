{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the first vertical slice.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
