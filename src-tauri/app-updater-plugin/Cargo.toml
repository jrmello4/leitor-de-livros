[package]
name = "tauri-plugin-app-updater"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-app-updater"
build = "build.rs"

[lib]
name = "tauri_plugin_app_updater"
path = "src/lib.rs"

[dependencies]
tauri = { version = "2", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
